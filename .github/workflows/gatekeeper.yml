name: Gatekeeper
on: [push, pull_request]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify exact public seed boundary
        run: python3 tools/verify_lkmini.py
