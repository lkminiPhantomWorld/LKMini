name: Gatekeeper
on: [push, pull_request]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify LKMini public seed
        run: python3 tools/verify_lkmini.py
